const MultimediaSelector = ({property, setProperty}) => {

console.log(property.multimedia); // = {images:["linkImage", "linkImage2"], videos:["linkVideo", "linkVideo2"]}

  return (
    <div>MultimediaSelector</div>
  )
}

export default MultimediaSelector